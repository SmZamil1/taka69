# Database — use Neon, Supabase, or local Postgres
DATABASE_URL="postgresql://user:password@localhost:5432/taka69?schema=public"

# Auth — generate with: openssl rand -base64 48
JWT_SECRET="change-me-to-a-long-random-string"

# App
NEXT_PUBLIC_APP_NAME="TAKA69"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_CURRENCY="TC"
NEXT_PUBLIC_STARTING_BALANCE="10000"

# Admin seed (used by prisma/seed.ts)
ADMIN_USERNAME="admin"
ADMIN_PASSWORD="ChangeMeAdmin123!"
ADMIN_EMAIL="admin@taka69.local"

# Optional: Vercel Cron secret
CRON_SECRET="optional-cron-secret"
